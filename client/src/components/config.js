// config.js

const baseURL = 'http://localhost:5000'; // Local development server
// const baseURL = 'http://13.61.63.5:5000'; // Your EC2 public IP and backend port
// const baseURL = "https://lostandfoundbackend-y9qs.onrender.com"; // (Uncomment if using Render in the future)
const config = {
  baseURL,
};

export default config;
